---
type: todo-phase
phase: 7
title: TODO — Phase 7 (F8 full admin + F9 platform polish)
status: pending
updated: 2026-08-04
tags: [todo, phase-7]
---

# Phase 7 TODO — full admin · Element Style Studio · platform polish

Parent: [[TODO Master]] · Previous: [[TODO Phase-6]] · Owner req 9, 10

## P0 — Kickoff
- [ ] **T7.0.1** SHARED_MEMORY reset + contracts carried forward — `pending`
- [ ] **T7.0.2** ui-ux-pro-max `--design-system` (admin/dashboard, Linear/Raycast crispness) + `--domain chart` (mandatory — the admin is chart-heavy) — `pending` · SO-4
- [ ] **T7.0.3** Read `webdev-standards/references/admin-tooling.md` — `pending`
- [ ] **T7.0.4** `docs/pipeline/phase-7/spec.md` with job slicing + budget lines — `pending`

## F8 — Full admin panel (blueprint 10)
- [x] **T7.1.1** Left sidebar (icon + label), max-1120px content on a 12-col grid — `done` · a GRID COLUMN inside `col-wide`, not a second fixed element: `BottomNav` is already `fixed inset-x-0 bottom-0` with its own z-index and safe-area padding, so a second rail would mean three z-index decisions to get wrong instead of none. A `tablist`, not a `nav` — these switch a panel in place and calling it navigation promises a screen-reader user a page change that never happens. `render` is a FUNCTION, so the section that is not open does not mount and does not fetch: measured, switching to Fiókok leaves 0 chart figures in the DOM. Measured at 1280px: twelve 67.33px columns, the rail exactly 240px (`--admin-sidebar-w`), the panel `min-width: 0` so a wide table scrolls inside its own wrapper instead of pushing the grid out, and no horizontal body overflow. **`check-tokens` nearly cost this its grid**: its undefined-utility rule matched `col-[a-z]+` — written for this project's `col-mobile`/`col-wide` and also Tailwind's prefix — so `col-span-3` and `col-start-2` were build failures. Measured by trying it. A gate that forbids the correct answer sends people to a worse one, so Tailwind's four real column utilities are named and a typo in `col-mobile` is still caught.
- [x] **T7.1.2** Stat cards — icon, odometer number (E16D), delta chip — `done` · four cards on the admin page. The delta chip is ABSENT when there is nothing to compare, rather than showing a green +0% — a zero delta on a window with no prior data is a claim the data cannot support, and it is the reassuring direction to be wrong in. Fixed `CountUp` on the way: it formatted with the BROWSER locale while the language comes from i18next, so a Hungarian UI on an en-US browser rendered "1,652" beside "1 652" everywhere else. Measured on screen afterwards: 85 000, Hungarian separator.
- [x] **T7.1.3** Metrics — `done`, and **it does not return DAU/MAU, because the product cannot measure it.** Checked against `pragma_table_info`, not assumed: no session table, no events table, no `users.last_seen_at`. `refresh_tokens` counts TOKENS — rotation mints a row per refresh, so one tab open all afternoon looks like a dozen people — and `audit_log` records notable events, which ordinary use is not. Adding `last_seen_at` would mean a write on every authenticated request, a write amplifier on the hottest path to power one number. So the field is `loggedPeople` (people who logged a workout or a meal), named so nobody has to read a comment to avoid the mistake. TWO CLOCKS kept apart: activity buckets on `local_date` — the user's own day, as 010 requires — while signups and coin velocity bucket on UTC; every series carries its `clock` and they are charted separately, because a UTC bar beside a local-date one claims a day boundary they do not share. "Active coach" means has an active client link, not holds the role. **Migration 025** adds the four time-leading indexes the schema had none of; `verify-025` asserts the QUERY PLANS by index name (3 of 4 COVERING, `SEARCH` not `SCAN`), and a `verify:gates` case drops one and watches the probe go red.
- [x] **T7.1.4** Accent area charts — `done` · four series through the existing `TrendChart`, which positions on TIME rather than index and refuses to draw under three points. Two defects came out of the walk-through: I wrapped it in a second `<figure>`, producing nested figures and the label announced twice (measured — the caption list showed both); and its "three training days" empty sentence followed the component onto the coin-velocity chart, telling an admin to log workouts. `emptyKey` defaults to the progress wording so no existing caller moved.
- [x] **T7.1.5** Dense tables — sticky header, row hover, sortable with `aria-sort` — `done` · **and the sticky header in this app had never stuck.** The moderation queue shipped `sticky top-0` on its `thead` inside a wrapper whose only job was `overflow-x-auto`. Measured with 12 rows: scrolling the page 600px moved the header 600px. `position: sticky` sticks to the nearest SCROLLING ancestor; `overflow-x: auto` makes an element a scroll container on BOTH axes (computed `overflow-y` is forced from `visible` to `auto`), so the header's reference was that wrapper — which had no height limit and therefore never scrolled (`scrollHeight === clientHeight`, measured). One `DataTable` in `ui/data/`, capped at the new `--table-max-h`, and the same probe now reports STUCK: the header holds at the wrapper's top edge while 25 rows scroll under it. `aria-sort` goes on the `<th>` (on the button it is ignored, quietly) and every sortable column carries `none` rather than nothing. New tokens: `--table-max-h`, `--table-row-h`, `--z-sticky` (below `--z-nav`, so a header cannot cover the way out of the screen).
- [x] **T7.1.6** User search + detail — `done` · `GET /admin/users` with debounced search, three sortable columns and keyset pagination through `lib/cursor.js` — which had ZERO admin callers while three admin list routes used three different dialects. Sort keys are a MAP from client key to SQL, so no column name ever reaches a query: a forged `?sort=email;DROP` is a 400, and `?q=%` is a 400 because `%` is a LIKE metacharacter and a search for it matches every account. `email` sorts on `lower(trim(u.email))`, the same expression its unique index is built on. The projection is the boundary: no `password_hash`, no `session_version`, no measurements, no food log. **The cursor was walked across a page boundary that falls inside a run of 20 identical timestamps** — 34 accounts, 4 sort keys, 2 pages each, zero repeats and every account reached, which is the branch a 14-row dev database with a 24-row page can never exercise.
- [x] **T7.1.7** Role grant / disable account / session reset — `done` · **eight files read `disabled_at` and NOTHING could set it.** Login, every authenticated request, publishing, restoring a withdrawn post, removing content and resolving a report all check it — so a coach posting things that should not be on the internet could have each post taken down one at a time, and keep posting. The revocation was already complete and correct: `getSessionVersion` answers -1 for a disabled account, which can never match a token's `sv`, and `invalidateSvCache` drops the 30-second read cache so the next request means the next one. Measured: a disabled account is thrown out on its very next request and cannot sign back in. **And the role route had a real unrecoverable race**: it guarded only "you cannot change your own role", as a pre-check outside any transaction, so two admins demoting each other at the same instant both passed and the product was left with NO admin — a state nothing recovers from, since every route that could mint one is behind `requireRole`. Now a named transaction with every guard, including the ACTOR's role, read inside the write lock. **The last-admin count I wrote for it was deleted, because it could not fire**: exercised with two concurrent demotions, the loser came back `not_an_admin` — the first write had already demoted it and the actor check re-reads the role from the database. The count can never fire, since the actor is always an enabled admin and never the target. What holds the invariant is the self-check plus the actor re-read, both seen to hold it; a guard that cannot fire still reads like a safety net, and the next person to relax one of the two real checks would believe something was behind them. `verify:last-admin` is in `check:all`, which goes 13 → 14 suites
- [x] **T7.1.8** Audited coin adjust — `done` · **it already existed and the audit gate proved it.** `POST /admin/users/:id/coins` (coins/routes.js) carries two rate limiters, a JWT role check, `assertAdmin`, and a THIRD role plus session-version check inside `adminAdjustCoinsTx` repeated in the guarded INSERT's own JOIN. Idempotency key composed as `adj:<adminId>:<clientKey>` and stored on the TARGET row so a support clawback cannot collide with the victim's own purchase keys. The sign is chosen in SQL by a CASE and `trg_coin_ledger_reason_shape` refuses a row whose sign and reason disagree. `check-admin-audit` confirms it reaches an audit row and re-checks the role under the write lock.
- [x] **T7.1.9** Exercise moderation queue (full, upgrading Phase 1's lite version) — `done` · review panel with the submission itself; decision controls moved out of the row; moderator media arm scoped to pending_review; rejection reason surfaced to the author. `verify:moderation` 16/16
- [x] **T7.1.10b** The queue has a screen — `done` · `MarketplaceQueue` on the admin page, and the loop was walked from the UI: a reader files a report, the admin sees it with the SNAPSHOT of the text as it was when reported, presses a decision, and the post answers 404 on the public surface and leaves the feed. The uphold button is disabled until a removal reason is typed — the schema and a trigger both refuse it anyway, but a form that lets you press a button it knows will fail wastes the moderator's time. **The screen cannot show a reporter even if it wanted to**: the server projection omits reporter identity entirely, and what it does show is `distinctReporters` — how many DIFFERENT people objected, which is the number that means something. Measured at 360 with real rows: one h1, zero overflow, zero off-scale type, zero rogue durations, zero targets under 44 px, zero raw i18n keys

- [x] **T7.1.10** Marketplace moderation: report, queue, takedown — `done` · **the gap the composer opened.** Migration 021 shipped `content_reports` with a careful shape and NOTHING in `src/` referenced it — grepped, zero files — and no route anywhere wrote `removed_at`. So the moment coaches could publish to the open internet, there was no way to report anything and no way to take it down. Three named transactions and four routes. **Resolution and removal are ONE transaction**: split in two there is a window where the report reads as handled and the content is still public, and that window is exactly when somebody is looking. The snapshot of the reported text is taken at REPORT time, so a moderator judges what was seen rather than what the author has since edited it into — and it is DESTROYED when the case closes, which a trigger enforces. A duplicate from the same reporter replays instead of adding a row, because the queue measures how many DIFFERENT people objected and letting one person file five times makes severity a function of persistence. The queue projection carries NO reporter identity. Measured: removing a PROFILE takes the coach's entire back catalogue off the marketplace on the next read with no sweep, the author still sees their removed post so an appeal is possible but never the moderator's note, and a removed post cannot be edited into something else. 29 assertions walked, 12 in smoke
- [x] **T7.1.11** `audit_log` entry for EVERY admin action, no exceptions — `done` · **a gate, not a habit.** `check-admin-audit.mjs` asserts three things about every route behind `requireRole(admin)`: it reaches an `INSERT INTO audit_log` (following the delegation into the named worker transaction rather than trusting the handler to look busy), it re-reads the actor role from the DATABASE, and its audit action string has exactly one writer. Today: 10 admin routes, 6 writes, 6 audited, 9 distinct actions, re-check 6 assertAdmin / 3 under-write-lock / 1 inline. Building it moved the route parser into `scripts/lib/parse-routes.mjs` — and the move found **four routes no gate had ever seen**: `POST /workouts/:id/finish`, `/abandon`, and the two `PUT /plans/:planId/.../order` build their handler with a factory instead of `asyncRoute`, so the old regex skipped them silently. `check-routes` had been printing 161 for a codebase with 165. All four happen to comply, which is luck, not a property — a clean result is a statement about coverage first. An unparseable registration is now a hard failure in both gates. Four mutation cases in `verify:gates` (13 passed).

## Element Style Studio (owner req 24)
- [x] **T7.2.1** Element list left / variant cards right with **live interactive preview** — `done` · `/admin/styles`, linked from the admin page. The preview is the playground's own exported `Demo` inside the same `VariantOverride` — a second harness would have been the eleventh reimplementation, and the two would drift until what an admin previews stops being what ships. Fixed the invalidation gap: `ElementStyleProvider` caches `[element-styles]` for 5 minutes and nothing invalidated it, so the acting admin was the ONE client that did not see their own change. Three elements (E23 Like/reaction, E24 Follow button, E27 Interval stage) are read by no component; their switches are disabled and the panel says why. `aria-live` removed from the panel after measuring that it would announce ~80 words per click.
- [x] **T7.2.2** Switch the active variant per element **E1–E27** × A–E — `done` · note the range: the TODO said E26 and so did the route's id regex, while the database, the catalogue and the fallback map all said 27. E27 was unsettable by anybody. Walked on screen: E1 D→A moved the active chip AND the list letter in one act, the toast fired, and the public read served the new variant.
- [x] **T7.2.3** Change applies GLOBALLY to every user, with no code change and no redeploy — `done` (backend) · `PUT /ui/element-styles/:id` writes the row, `GET /ui/element-styles` is public because the login screen is styled by it, and `ElementStyleProvider` merges it over the curated fallback. Measured end to end: an admin PUT on E27 returned 200 and the public read served the new variant. **Two defects found and fixed getting here** — the id regex enumerated E1–E26 so E27 (migration 012) could not be set by anybody, and the limiter sat above `requireRole` so any signed-in user could empty the admin's budget. The acting admin's OWN tab still waits up to 5 minutes (`staleTime`, nothing invalidates `[element-styles]`); that invalidation belongs to the studio screen, T7.2.1.
- [x] **T7.2.4** Every change audited (actor, element, old→new variant, timestamp) — `done` · `ui.element_style.update` with `{elementId, from, to}` in `detail`. `target_id` stays NULL because `audit_log.target_id` is INTEGER and the element id is TEXT — 021 records a design that deadlocked its own transaction by pointing it at a TEXT key. `check-admin-audit` asserts the row exists by following the route into its writer, and that the action string has exactly one writer.
- [x] **T7.2.5** Admin role re-checked in the DB at execution time, not just from the JWT — `done` · and now enforced rather than remembered: `check-admin-audit` fails the build for any route behind `requireRole(admin)` that authorises from the token alone. Ten admin routes today: 6 via `assertAdmin`, 3 under the write lock, 1 inline read. A mutation case in `verify:gates` deletes the re-check and confirms the gate goes red.

## F9 — Platform polish
- [ ] **T7.3.1** PWA install + offline shell — `pending`
- [ ] **T7.3.2** Offline indicator with queued actions — `pending`
- [x] **T7.3.3** i18n HU/EN/DE completion sweep — every string keyed, `check:i18n` green — `done` · the sweep was a MEASUREMENT, not a pass over the files. Scanned every .tsx for literal JSX text nodes outside the exempt QA pages: **16 hits, all 16 false positives** — TypeScript generics (`Promise<void>`) and comparison operators read as text between `>` and `<`. No hardcoded visible string exists. Then measured what the gate could NOT see: it caught a whole bundle copied, but not a single key pasted into one. Across 764 keys — `hu==en`: 9, `hu==de`: 9, `en==de`: **26**. English and German share vocabulary (Admin, Import, Playground), so an en/de rule is 26 false positives and an allowlist nobody maintains; Hungarian shares almost none, so it is the pivot. The nine legitimate ones (proper nouns, SI units, EMOM/AMRAP, a pure format string) are named with reasons. **One real gap found**: `studio.inertShort` read inert in Hungarian, which is not a Hungarian word. The first version of the rule required all THREE to match — conservative and useless, because the common case is a paste into one bundle; proved by trying it and watching it stay green.
- [x] **T7.3.4** GDPR data export — owner-bound, anti-IDOR — `done` · `GET /me/export` takes NO id: not a path parameter, not a body field, not a query string. The anti-IDOR argument is that the object-level question is never asked, because there is only ever one object — and an admin cannot pull somebody else's record through it, deliberately, because a support tool that produces an export on demand is a tool for reading people's health data with a legitimate-sounding button. ONE worker transaction, not thirty pool calls: each pool call is a different thread with its own read snapshot, and a legal artefact cannot be thirty moments stitched together. `Content-Disposition: attachment` + `no-store`, because an export that renders in a tab lives in the cache and the back button. Walked: 30 sections, the health data present, and no `$argon2`, `password_hash` or `token_hash` anywhere in the payload.
- [x] **T7.3.5** GDPR account deletion (health data = sensitive category) — `done` · **the capability was measured before the route was written**, because this project has found the same shape three times: 55 FK columns across 41 tables reference `users`, 39 CASCADE and 16 SET NULL, and a real delete succeeds with the audit row surviving at `actor_id = NULL` — which is exactly what 018 repaired. Nothing is deleted by hand; a hand-written cascade goes stale the first time somebody adds a table, and it goes stale silently. Step-up: the password is re-typed AND `confirm: "DELETE"` is required. The **last-admin guard here CAN fire** (unlike the two deleted from other transactions) because the actor IS the target — the sole admin erasing themselves would leave the product with none. One defect found and fixed on the way: `exercises.owner_id` is ON DELETE CASCADE and approval never clears it, so a coach with one PROMOTED exercise would take it out of the shared library for everybody; erasure now unlinks it, which is what erasure means. `verify-gdpr`: 19 assertions, an account created and genuinely erased.
- [x] **T7.3.6** Capacitor safe-area insets verified on real device sizes — `done`, and the verification found a real gap: **every use of `env(safe-area-inset-*)` in the codebase was VERTICAL.** `.screen-x` — the horizontal gutter every screen in the product uses — was a flat 16px. `viewport-fit=cover` is set, which is what makes the insets report anything AND what lets the layout extend under the cutout, so turn a notched phone sideways and the first character of every line sits under the notch. A fitness app is used sideways: a phone propped against a rack is how somebody reads their next set. Fixed with `max()`, not addition — the inset REPLACES the gutter when larger, so a 44px cutout does not get 16px stacked on top of it, and devices with no inset are unchanged. `check-safe-area.mjs` makes it a property rather than a one-off check: cover mode present, `.screen-x` accounting for left/right, and every edge-pinned fixed element accounting for its edge (with an EXEMPT map that needs a written reason). All three rules were broken on purpose and seen to fire. Its summary line used to print `viewport-fit=cover` unconditionally — a header stating the thing the body was refuting — and now reports what it measured.
- [x] **T7.3.7** Cmd+K command palette on desktop — `done` · **it already existed, and I overwrote it before checking.** `git status` said `M`, not `??`. Restored from HEAD and EXTENDED instead — the original already had Cmd+K, Escape, role filtering, desktop-only (`lg:flex`) and no animation with the Bible frequency-table reason written out. Two gaps closed: it knew **five of the product's eighteen screens** (home, library, settings, playground, admin), and closing it dropped focus on `<body>`, costing a keyboard user their place every time they opened it and changed their mind. Now 16 commands for an admin, and focus returns to the element it was captured from — measured: `/library` → palette → Escape → `/library`. One defect from the walk: typing "nutrition" into the Hungarian UI returned NOTHING. The label was "Táplálkozás" and the keywords carried every word except the English name of the screen, while the comment above them claimed they carried both languages. The ROUTE is searchable now, which fixes every entry at once and in every language because a path is already the English name of the thing.
- [x] **T7.3.8** Encrypted backup + documented, tested restore drill — `done` · `scripts/backup.mjs` uses `VACUUM INTO` (never `db.backup()`), and the encryption was **measured before the script was written** because nothing in this repo had established that SQLCipher carries the key through a VACUUM: the output has no `SQLite format 3` header, opens with the key with matching row counts and `user_version`, and is refused without it. Safe against a live database — `VACUUM INTO` takes a read transaction and WAL readers do not block writers. The backup VERIFIES ITSELF and **deletes the file on any failure** rather than leaving something that will be trusted at 3am. `scripts/restore-drill.mjs` is the half people skip: it restores a COPY of the newest backup and asks the five questions that matter — does today's key open it, is the schema version one this checkout knows, are the rows actually there, does a real product query run, and does `integrity_check` plus `foreign_key_check` pass. 11 assertions, all green. **`backups/` was NOT gitignored** — the first backup this repo ever took sat untracked, 6.8 MB, one `git add -A` from the history.
- [x] **T7.3.9** `scripts/rekey.mjs` runbook documented — `done` · **the TODO said the script existed from BASE and it did not** (third time this phase that a claimed capability was absent). Written, and the writing found the thing that matters: `sqlcipher_export` is NOT available on this engine, so in-place `PRAGMA rekey` is the only path — and **`PRAGMA rekey` refuses to run in WAL journal mode**, which is one of this project's four mandatory pragmas. The production database is therefore ALWAYS in the single mode rekey rejects. Without that discovery the script would have failed on the real database at the worst possible moment: server stopped, backup taken, operator committed. It now switches to DELETE, rotates, switches back, and VERIFIES the mode is `wal` again — leaving production in DELETE would cost every concurrent reader and nothing would announce it. Preconditions are enforced (fresh backup < 1h, current key works, minimum key length) and `.env` is updated SECOND, deliberately: had it gone first and the rekey failed, the config would name a key the file does not use. `verify:rekey` proves the whole sequence on a scratch database, both directions — the new key opens it AND the old one does not, which is the assertion that catches a rekey that silently no-ops.

## Security
- [x] **T7.4.1** Admin routes: `requireRole` + DB role re-check + step-up where destructive — `done` · the first two are enforced by `check-admin-audit` rather than remembered (10 admin routes, 6 assertAdmin / 3 under-write-lock / 1 inline). **Step-up needed the question answering first**: which admin action is destructive? Enumerated — exercise moderation, disable, role change, coin adjust, takedown and style variant are ALL reversible, and the only irreversible act in the product is a person erasing their own account, which already asks for a password. So step-up does not answer "can this be undone" but **"what does this HAND OVER"**, and there is exactly one such action: granting `admin`. It is what a stolen session does first, because a second admin it controls SURVIVES the original being revoked. Demotion deliberately needs no password — asking for one to take power away, when none is asked to exercise it, gets the incentive backwards. `verify:stepup`: 8 assertions, both directions.
- [x] **T7.4.2** Admin endpoints on their own rate tier, all abuse signals logged — `done` · measured first: **eight admin routes shared ONE 120/15min per-IP limiter**, reads and writes together. Opening the dashboard costs three requests (stats, metrics, users), so a working session spent the write budget on looking at things — the limiter meant to bound damage was exhausted by the page that shows there is none. And IP-only: that stops one machine, not one principal with a laptop, a phone and a VPN, which is the shape of a compromised admin session. Now four tiers: read 600 IP / 300 account, write 120 IP / 60 account. **The abuse signal was going nowhere**: reaching `assertAdmin`'s refusal means somebody presented a VALID token whose admin claim the database disagrees with — a revoked role, a disabled account, or a token minted before either — and it was answered with a silent 403. It now logs a warning naming the user, the route and what the database actually says. A log line rather than an audit row, because `audit_log` records what happened and a refused request did not happen — but it is what an operator greps for after a laptop goes missing.
- [x] **T7.4.3** Export/delete flows ownership-bound and rate-limited — `done` · both scoped to `req.user.id` with no id anywhere in the request. Export: 30/h per IP + 6/h per account (it is the most expensive read in the product). Delete: 20/h + 5/h, tiered on the PASSWORD attempt rather than the deletion, because the endpoint is a password oracle otherwise. `check-gdpr` holds the export list to the schema — every table with a foreign key into `users` is exported or named in EXEMPT with a reason — and **it PREPARES every statement**, which found six that would have thrown for every person who asked for their data (`body_measurements.user_id` is `client_user_id`, `meals` has no user column at all, `workout_plans.coach_id` is `author_user_id`). The first version of that gate checked only that each statement was correctly BOUND, and went green over all six.
- [ ] **T7.4.4** Full pre-deploy security checklist walked — `pending`

## Phase gate
- [ ] **T7.5.1** build + smoke + `npm audit` green — `pending`
- [ ] **T7.5.2** Screenshots 360/1440 + Bible line-by-line audit — `pending`
- [ ] **T7.5.3** Webview E2E ✅/❌ matrix — `pending`
- [ ] **T7.5.4** **Full regression sweep across the entire app**, all features, all green — `pending` · E2E protocol final sweep
- [ ] **T7.5.5** Brain updated + sync; SHARED_MEMORY pruned — `pending`

## Related
[[TODO Master]] · [[TODO Phase-6]] · [[TODO Phase-8]]

## F-C — The coach-side composer (the Phase 6 write surface)

Spec: [[Composer Spec]] — a 13-agent adversarial pass, 60 defects (1 fatal, 18 severe). The public
marketplace has shipped read-only since Phase 6: a coach cannot create a profile or publish a post,
and the two posts that exist were inserted by a script through the DB facade.

**OWNER DECISIONS 2026-08-09**, taken against the defect-clustering evidence:

- **Media: cover image only.** Upload, delete, author-side view. The gallery, image reordering and
  every media UPDATE are cut, and replacing a cover is DELETE then POST — two operations that are
  each already atomic. The media surface carried ~40% of all defect weight and the only FATAL
  finding; `post_media` has zero UPDATE triggers and an unfrozen `post_id`, so every UPDATE not
  written is an IDOR that cannot exist.
- **Scheduled publishing: CUT.** Measured — `PUBLIC_POST` contains no `unixepoch()` comparison, so
  a future `published_at` is publicly readable the moment it is written, and the daily quota window
  has no upper bound, so posts scheduled ahead would eat the whole allowance every day until then.
- **Handle rename, autosave and the handle-availability endpoint: KEPT**, against the spec's
  recommendation. They ship WITH their defects fixed rather than avoided — that is the cost the
  decision accepts, and it is written here so the bill is visible rather than discovered later.
- **Client food log stays private** (T4.5.1) — unrelated to the composer, decided in the same pass.

### Blocker, verified before anything else was planned
`guidelines_versions`, `guidelines_acceptances` and `public_policy` exist ONLY in migration 021.
Nothing in `src/` reads or writes them — grepped, not assumed. There is no way for anyone to accept
the community guidelines, so the publish gate would deny every coach in the product. The consent
routes are therefore T7.C.2, not an afterthought.

- [x] **T7.C.1** `022_composer.sql` — owner-scoped `write_uid` idempotency on `coach_posts` and `post_media`, `content_sha256` for intent comparison on replay, `row_version` for optimistic concurrency (`unixepoch()` is one-second granular, so a timestamp guard silently no-ops inside its own second), and the restore-standing trigger — `done` · schema v22. Every claim the migration rests on was checked against the LIVE schema first — 16 of 17 held and the seventeenth was the checker counting all triggers where it meant UPDATE triggers, so the spec was right and my probe was sloppy. `verify:022` is 32 assertions and asserts the ACCEPTED cases with the same weight as the refusals, because the trigger 022 replaces was refused for being too eager: a paragraph reflow changes body_src and can leave body_doc byte-identical, and a doc_version bump moves the doc alone, so 021's exclusive-or aborted ordinary edits and would have frozen the whole published corpus. Mutation-tested: deleting the identity-freeze trigger produces exactly 4 FAILs, restoring it returns 32/32
- [x] **T7.C.0** Four LIVE defects in the shipped Phase 6 read surface — `done` · found while reading the composer spec, none of them a composer question. **(1)** `formatPrice` divided by a hardcoded 100; `public_currencies.minor_units` is 0 for HUF, so every Hungarian price on the open internet rendered at ONE HUNDREDTH of its value — a 45 000 Ft programme reading "450 Ft". Three things claimed to know the scale (the column, the seed, the formatter) and only the column did. **(2)** The media serve route piped a read stream with no `'error'` listener, outside `asyncRoute`'s promise chain: a missing file was an uncaughtException and a process restart, not a 404 — demonstrated in an isolated process rather than asserted. **(3)** `/public/coaches` returned `c.user_id` as `nextCursor`, anonymous enumeration of account ids, while `PUBLIC_PROFILE_COLUMNS` omits `user_id` precisely to prevent that; **(4)** `/public/posts` returned the internal rowid, which `public_id` exists to keep unaddressable and which verify-021 separately asserts. `src/lib/cursor.js` already existed and was already used by the exercise routes — the NINTH instance of this project's second defect class, again by omission. **Two smoke assertions searched for the KEY NAMES** `"userId"`/`"user_id"` and stayed green the whole time, because the id shipped as the VALUE of `nextCursor`

- [x] **T7.C.0b** The public smoke assertions had nothing to be about — `done` · every anonymous marketplace check ran against an EMPTY corpus: the suite boots a fresh database and there are no marketplace write routes, so nothing had ever published anything. Including the flagship one — "a signed-in visitor gets BYTE-IDENTICAL bytes to an anonymous one" reported `anon 30b, auth 30b`, and thirty bytes is `{"posts":[],"nextCursor":null}`, so the defining property of the public surface was being proved by comparing two empty lists. `seedPublicCorpus()` now publishes a profile and two posts through the server's own database file — the schema's publish gates and coherence triggers still police it, and the body goes through the REAL parser so no assertion passes over a document the product cannot produce. The same check now reads `anon 1107b, auth 1107b`. Smoke 488 → 494

- [x] **T7.C.2** Guidelines consent + composer context — `done` · `src/public/compose.js`, mounted BELOW `csrfProtection` and deliberately NOT inside `public/routes.js`: that file is mounted above the CSRF middleware and `check-routes` FAILS the build on `requireAuth`/`req.user` appearing in it, so a write placed there would have forfeited all three CSRF layers while the gate stayed green — it would have passed by being in the file the gate exempts. `GET /compose/context` is ONE statement (three pool calls are three worker threads with three read snapshots, which renders a screen contradicting itself) and binds the caller ONCE through a CTE rather than repeating it in ten positional parameters. It serves the product limits so the editor cannot keep a second copy — 021 itself names the split: its column CHECKs are storage-safety bounds, the product bounds live in code. `POST /compose/guidelines/accept` takes an ECHOED version that can only cause a refusal, since what is stored is derived from `active = 1` inside the statement; a stale echo is 409 naming the version now in force, and a replay returns the ORIGINAL `accepted_at` via `INSERT OR IGNORE` on the natural key — `OR REPLACE` would rewrite evidence of when somebody agreed to something. **The fresh-insert path is asserted, not assumed**: smoke checks `guidelinesAcceptedAt === null` before accepting, because a suite run against an already-consenting account proves the replay and never the write. 12 new smoke assertions, 494 → 506
- [x] **T7.C.2b** The shared modules the composer stands on — `done` · `shapes.js` (the identifier shapes, three of them lifted out of `public/routes.js` where the city pattern alone was written out THREE times) and `text.js` (`sanitizeDisplayText`). **021 named that function and it had never existed** — grepped, zero hits, and no `check-public-text.mjs` either. The column comment says the strip is "a CODE control, stated as one... and not pretended to be a schema control here", and then nobody wrote the code; it was unreachable only because no route wrote a display name, which is why it had to land before the first one does. The set of invisible characters is EXPORTED from `markdown.js` rather than retyped, and the extraction was proved equivalent across all 65 536 BMP codepoints before it was written to disk — the check caught that U+000D was in the shipped set and missing from my escape form. **Measured, not assumed: SQLite trim() strips ASCII space and nothing else**, so NBSP, U+3000, TAB and NEWLINE all survive it and a display name of two non-breaking spaces satisfies `length(trim(x)) BETWEEN 2 AND 120` — a blank entry in the public directory that no schema control can refuse. `verify-022` asserts the column ACCEPTING that name, so what deleting the code control gives back is visible. Also: `HANDLE_RE` and the four-clause column CHECK agree on all 1340 candidates, asked of both rather than read side by side. 32 → 44 assertions

- [x] **T7.C.3** Coach profile: create, edit, publish, unpublish — `done` · five routes and FOUR named worker transactions, never one `setProfile(flag)` — publish carries a standing gate that unpublish deliberately does not, `listed_at` is written only on the publish path, and create leaves `published_at` out of the INSERT column list entirely so the publish trigger cannot fire on an insert. A PUT rather than a PATCH: every field required, null means cleared, so absent-versus-null merge semantics never enter the surface. **What is ABSENT from the schemas is the control** — `verified_at`, `published_at`, `listed_at`, `removed_at`, `user_id` and `handle` are all rejected by name by `.strict()`, asserted one field at a time. The city and specialty checks sit ABOVE the first write specifically because they are natural to write after the DELETE that clears the old set, where a conditional return would COMMIT a profile stripped of its specialties. `buildBody`/`buildBio` is the one producer of the four columns that must agree, and it runs on the MAIN THREAD so no worker ever parses. **Unpublish reports how many posts went dark**, because `PUBLIC_POST` requires a live profile and there is no sweep to watch. Two of my own defects caught by exercising it: `sendBodyError` read `err.reason` where `MarkdownError` carries `.code`, so every body failure would have arrived as a generic `invalid_body`; and a smoke assertion compared two `undefined` listed_at values and reported PASS while publish was being refused outright. The publish gate chain is now asserted BEFORE it is satisfied — needs_guidelines, then too_new with its eligibleAt — because ageing the account first would have quietly deleted the assertion that the gate exists
- [x] **T7.C.4a** Handle SQUATTING closed at the schema — `done` · migration 023, schema v23. **The attack was reproduced first**: one account, ONE PROFILE NEVER PUBLISHED, six renames — six desirable handles locked for 365 days, refused to a second account 6/6 and reclaimable by the squatter 6/6, with the write limiter allowing thousands a day from one free registration. The fix came from asking what the cooldown is FOR: `retired_handles` stops impersonation via stale links, and that risk exists only for a handle somebody could once reach. So retirement now keys on `listed_at` — an unpublished profile releases its handle immediately, and locking one costs a publish, which costs an accepted guidelines version, an account old enough, and a quota slot. A 30-day rename cooldown, stamped BY THE DATABASE so no route can forget it, closes publish-rename-publish-rename; it does not apply to unpublished profiles, because fixing a typo before anyone can see the page is the most ordinary act in the feature. `verify:023` proves BOTH halves — a fix that also deleted the protection would pass an "attack no longer works" assertion perfectly

- [x] **T7.C.4** Handle rename + availability check — `done` · all three named defects fixed structurally. IDOR-1 closed by 023 (retirement keys on `listed_at`) plus the 30-day rename cooldown. RACE-5/REPLAY-10 closed twice: the handle is absent from the profile PUT, and the rename carries `from` — a tab whose belief is stale is refused with the TRUE current handle, and a replay returns the original result without writing, burning no cooldown and leaving no second audit row. IDOR-2: `GET /compose/handle` answers ONE boolean; reserved/taken/cooling are indistinguishable, rate-limited per account as well as per IP. **Migration 024** moved the cooldown predicate into `coach_handle_rename_eligibility` so the trigger and the route read ONE definition — the alternative was the route pre-checking a copy and the two drifting, with the drifting copy being the one that says YES. No `changes === 0` probe on the UPDATE: IMMEDIATE transaction, row read inside it, the guard could not fire (same reasoning that deleted the last-admin count). Walked on the screen: uppercase and spaces corrected to `anna-nagy-coach` live, a reserved word answers exactly like a taken one, the rename applied and the public page moved (old 404 / new 200), the second rename came back as a translated sentence with a real date. verify-024: 37 assertions, both directions on every rule.
- [x] **T7.C.5** Posts: create draft, edit, publish, withdraw, restore, list, read-as-author — `done` · seven routes, five named worker transactions, **and the loop is closed**: a coach writes a post through the API and a stranger reads it with no session, verified end to end. The recurring shape is object-first — a post that is not yours answers 404 BEFORE a word is said about your standing, or the error text distinguishes "somebody else's post" from "no such post". A malformed id is ALSO 404, because a 400 on the shape plus a 404 on an unknown id is together an oracle for what a valid id looks like. **The manage list must NOT compose `PUBLIC_POST`** — that predicate requires `published_at IS NOT NULL`, so reusing it returns zero drafts and the bug reports itself as an empty state. `row_version` rather than a timestamp guard, because `unixepoch()` is one-second granular and a timestamp comparison does nothing in exactly the case it exists for; a stale save answers 409 CARRYING the current row so the client can show it beside the draft the coach still has. The per-kind rules are read from the stored `post_kinds` row, never restated in a `z.enum`, so adding a kind stays an INSERT. `kind_key` is frozen on edit, which makes `trg_post_kind_shape_upd`'s missing `active = 1` clause unreachable rather than arguable. Withdraw carries NO `removed_at` term on purpose — `deleted_at` is absent from `trg_post_frozen_while_removed_upd`'s column list precisely so an author can always take their own content back — and restore returns the post at its ORIGINAL feed position for no quota, which is the anti-bump property working. Two more false passes of mine closed: a restore assertion comparing two `undefined` timestamps, and a "no markdown source" check that passed on a 404 where every key is absent · `row_version` for lost updates; the body XOR trigger replaced, because as specified a `doc_version` bump makes every existing post permanently uneditable including title-only edits, with no request that succeeds and no escape through DELETE+INSERT
- [x] **T7.C.6** Autosave — `done` · KEPT by owner decision, and RACE-7 is genuinely closed rather than avoided. The race: a create is in flight carrying an idempotency key, the coach keeps typing, a blur fires a second create with the SAME key, the server correctly REPLAYS and answers with the post as it was created, the client navigates — and the URL change reads as success while everything typed since the first request left is gone. **Three things close it and all three were needed.** (1) SINGLE FLIGHT: the second trigger raises a flag instead of sending. (2) COALESCED FOLLOW-UP: content that moved during a save gets exactly ONE more save — not none, which loses it, and not one per keystroke. (3) The create adopts the snapshot it actually SENT, never the response — a replayed create answers with the original post, and adopting that would tell the hook the newest text was already saved. **A fourth defect was found on the way**: the editor re-seeded from the server on EVERY refetch, so the server's copy was written over the editor continuously while somebody typed into it — rare enough to look like a glitch with a manual save, and the last step of RACE-7 with autosave. Seeded once per post id now. Autosave calls the SAME `submit` the button does, so there is one answer to create-versus-update and one idempotency key. `verify-autosave`: 18 assertions, driving the race at the millisecond rather than hoping to win it in a browser — and the transcription it uses is CHAINED to the hook's source, because an audit carrying its own copy of what it audits is a green line about a thing that no longer ships.
- [x] **T7.C.7** Cover media — `done` · ONE cover per post and NO replace: the review put 40% of the whole defect corpus here including its only FATAL finding, so replacing is delete-then-post and there is no UPDATE of `post_media` anywhere in the product — every UPDATE not written is an IDOR that cannot exist. **The namespace 021 promised and nobody built**: three artefacts described three file layouts, `resolveStoredPath` took one argument and knew only the private shapes, and the serve route joined `MEDIA_DIR` flat — safe only while two key regexes happened not to overlap. Public files now live in their own subtree and a private key cannot be resolved from the public namespace at all. `validateImage` extracted so ONE sniff and ONE allowlist serve two policies; there were already four sniffers in this repo and three do not re-encode. **The multipart CSRF check had THREE copies** (exercise media, chat attachments, progress photos) and the cover was about to be the fourth — compared first: identical in behaviour, different only in line breaks, so nothing had gone wrong yet. Collapsed into `auth/middleware.js` beside the rule it narrows. Measured end to end with a real JPEG: two distinct 41-char keys, files in the public subtree and not the flat one, a DRAFT cover NOT served (asserted before publishing, because after it that can never be observed again), both variants served anonymously with the card genuinely smaller, replay comparing BYTES rather than presence, an SVG renamed and re-typed as a JPEG refused on magic bytes, and a delete that removes both files with the process surviving the next request
- [x] **T7.C.8** Server-side preview — `done` · `POST /compose/preview` calls the SAME `buildBody` the save path calls, so a preview and a published post are the same function of the same input. The alternative — a client-side markdown renderer for the preview and the server parser for the save — is two grammars that agree until the day they do not, and that day is a coach publishing something they never saw. Writes nothing, reads nothing, so there is no id and no 404. Measured: a script tag previews as TEXT and a `javascript:` URL never becomes a link node, because the real parser runs. **The two surfaces carry different bounds and the preview enforces the one that will be SAVED** — the same 9 000 characters is `too_long` as a bio and fine as a post, since `bio_src` is capped well below a post body; previewing at the post bound would show an author text the save is about to refuse. My first bound test did not measure that at all: 9 000 characters on ONE line hit the per-line limit instead, so both surfaces refused it for the same unrelated reason
- [x] **T7.C.9** Frontend composer screens — `done` · three screens — the desk, the profile editor, the post editor — plus `useCompose.ts`. **The standing ladder is the SERVER's**: the desk reads the flags the context endpoint computed in one statement and renders the first unmet step, rather than re-deriving four trigger-enforced preconditions in TypeScript where the copy would be the one that is wrong. **There is no markdown renderer on this side at all** — the preview endpoint runs the same `buildBody` the save runs and its tree goes through the same `DocRenderer` the public page uses, so preview and published page cannot disagree; measured in the browser, a typed heading comes back as a real heading and a link as a real anchor carrying `rel="noopener noreferrer nofollow ugc"`. Every bound comes from the server, so the counter cannot promise what the validator refuses. The idempotency key lives in a REF, not state: regenerating it on a re-render is how a retry quietly becomes a second post. A stale save renders the server's copy beside the coach's with a button to take it. **One blocker found and fixed a layer down**: `ApiError` kept only status/code/requestId and DISCARDED the rest of the envelope, so every 409 fact the backend carefully returns — which version to accept, when eligibility starts, how many slots remain, the current row — was being deleted below any screen that could use it, and the symptom would have been a designed refusal rendering as "request failed". Measured at 360: three screens, one h1 each, zero overflow, zero off-scale sizes, zero rogue durations, zero small targets, zero raw i18n keys. 556 → 647 i18n keys × 3
- [x] **T7.C.7b** Cover upload wired into the editor — `done` · `apiWithRefresh` now passes FormData through untouched: stringifying it produces the literal "[object FormData]" — a request that looks healthy and uploads nothing, a bug this project already paid for once in its smoke harness — and the Content-Type must NOT be set, because the browser writes the multipart boundary itself. Handled in the ONE helper rather than a second uploader beside the screen. A separate AUTHOR-side serve route exists because a draft cover cannot come from `/public/media/:key`, which gates on `PUBLIC_POST` and rightly refuses it: two routes, one file, two different questions — "may the world see this" and "is this yours". **A real defect found by walking the UI rather than trusting the 201**: the upload answered 201 Created and nothing appeared on screen, because `useComposeInvalidate` invalidated `compose-posts` (the list) and never `compose-post` (the one being edited), so every write to the open post left the editor showing stale data. To the user that reads as "it did not work". Verified after the fix: the browser fetched and DECODED the image from the author route at 640×420, with the size line, the remove button and the delete-then-upload note. Also fixed a guard of mine that matched the DELETE route's path and silently skipped adding the GET — the same shape as the TODO guard that matched its own cross-reference

- [x] **T7.C.10** Every gate SEEN TO FIRE by breaking what it guards — `done` · two new gates and a harness that proves all of them. `check-body-writes` closes the spec's §5.3 hole for the project's number-one defect class: no SQLite trigger can check that `body_doc` is the PARSE of `body_src`, so this asserts the four columns move together, that the parser has exactly one caller, and that `doc_version` is never a typed literal. `verify-gates` plants a real defect in the real source, runs the real gate, restores the file in a `finally`, and asserts byte-identity afterwards — 9 cases across three gates. **And it immediately found that the ADR-0005 gate could not see the shape ADR-0005 is about**: the `changes === 0` exemption was applied after ANY write, when its whole argument — "the statement wrote nothing, and committing nothing is a no-op" — only holds if nothing else has been written yet. A conditional return after a SECOND write commits the first, which is precisely the composer review's only FATAL finding. The gate now requires exactly one write to precede the exemption. **My first mutation was also wrong and that is recorded**: it planted the return after the FIRST write and reported the gate as broken, but a `changes === 0` return there genuinely commits nothing, so the gate was right and the test was not. `check:all` 11 → 13 suites

- [x] **T7.C.11** The composer flows — `done` · **E15 shipped with five variants, an undo affordance and a polite live region, and the only file that ever rendered one was the variant playground.** No screen could raise a toast, so every mutation in the product finished in silence — and a save that works and says nothing is indistinguishable from a save that did nothing, which is exactly the shape of the cover upload that answered 201 while the editor showed stale data. `ToastHost` gives it a home; `--z-toast` and `--content-pad-b` were both nearly re-derived by hand and are both already declared. Added with it: Ctrl/Cmd+S saving, a `beforeunload` guard that is the honest cost of cutting autosave, a save button that says 'Mentve' and disables itself when there is nothing to save, counters that turn colour only near the bound, and **an Undo on withdrawal** — measured end to end, one tap returns the post to its ORIGINAL feed position for no quota, so the undo genuinely costs nothing. Two hardcoded English strings found inside E15 (`Undo`, `Undone`) that `check-i18n` cannot see, because it audits `t()` calls against the bundles and a bare JSX literal is invisible to it — the same blind spot that once let a hardcoded 'Loading' through. **And two defects I reported that did not exist**: the browser pane's page is `hidden`, which suspends requestAnimationFrame, so every Motion entrance freezes at its initial values — I read `scale(0.96) translateY(24)` off a settled toast with `getBoundingClientRect`, which reports the TRANSFORMED box, and concluded it overlapped the nav and had 42px buttons. Measured with `offsetHeight`, which is layout: 44px buttons, 80px padding clearing a 65px nav. The instrument was wrong, not the code
